# ##############################################################################
# Bspwm setup general keybindings
# ##############################################################################

# For maximal goofiness: Cmd => ctrl, Ctrl => mod4, Opt_L => mod1, Opt_R => mod5
ctrl + mod1 + shift + {Left,Down,Up,Right}
  bspc node -s {west,south,north,east}
ctrl + mod1 + shift + {1-8}
  id=$(bspc query -N -n); bspc node -d ^{1-8}; bspc node -f ${id}
ctrl + mod1 + shift + f
  bspc node -t ~fullscreen
ctrl + mod1 + shift + space
  bspc node -t ~floating
ctrl + mod1 + shift + r
  pkill -USR1 -x sxhkd; bash ~/.config/bspwm/bspwmrc
ctrl + mod1 + shift + l
  bash ~/dotfiles/scripts/rofi-shutdown.sh
ctrl + mod1 + shift + Return
  kitty

# ##############################################################################
# Bspwm setup MacOS style keybindings
# ##############################################################################

ctrl + q
  bspc node -c
ctrl + Tab
  rofi -modi window -show-icons -show window
ctrl + space
  rofi -modi combi -show-icons -show combi -combi-modi drun,run
ctrl + shift + 3
  gnome-screenshot
ctrl + shift + 4
  gnome-screenshot -a
XF86MonBrightnessDown
  notify-send "***TODO*** XF86MonBrightnessDown"
XF86MonBrightnessUp
  notify-send "***TODO*** XF86MonBrightnessUp"
XF86LaunchA
  notify-send "***TODO*** XF86LaunchA"
XF86LaunchB
  rofi -modi combi -show-icons -show combi -combi-modi drun,run
XF86KbdBrightnessDown
  notify-send "***TODO*** XF86KbdBrightnessDown"
XF86KbdBrightnessUp
  notify-send "***TODO*** XF86KbdBrightnessUp"
XF86AudioPrev
  notify-send "***TODO*** XF86AudioPrev"
XF86AudioPlay
  notify-send "***TODO*** XF86AudioPlay"
XF86AudioNext
  notify-send "***TODO*** XF86AudioNext"
XF86AudioMute
  notify-send "***TODO*** XF86AudioMute"
XF86AudioLowerVolume
  notify-send "***TODO*** XF86AudioLowerVolume"
XF86AudioRaiseVolume
  notify-send "***TODO*** XF86AudioRaiseVolume"
XF86Eject
  bash ~/dotfiles/scripts/rofi-shutdown.sh
